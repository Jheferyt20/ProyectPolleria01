<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectPolleria"
        tools:targetApi="31">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCKqaY_PmDt66YWihefC2AOICygQ0OM3J0" />


        <activity
            android:name=".Home"
            android:exported="false"></activity>

        <activity
            android:name=".DatosActivity"
            android:exported="false"
            android:launchMode="singleTop"></activity>
        <activity
            android:name=".politica_empresa"
            android:exported="false"></activity>
        <activity
            android:name=".descripcion_c"
            android:exported="false"></activity>
        <activity
            android:name=".Activity_carrito"
            android:exported="false"></activity>
        <activity
            android:name=".Google_maps_api"
            android:exported="false"></activity>
        <activity
            android:name=".Busquedaa"
            android:exported="false" />
        <activity
            android:name=".Activity_register"
            android:exported="false" />
        <activity
            android:name=".Busqueda_combos"
            android:exported="false"></activity>
        <activity
            android:name=".delivery_distritos"
            android:exported="false"></activity> <!-- Actividad principal ahora es .Home -->
        <activity
            android:name=".Activity_login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Otras actividades de tu aplicación -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>